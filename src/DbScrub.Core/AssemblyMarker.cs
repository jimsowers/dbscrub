namespace DbScrub.Core;

/// <summary>
/// Empty type whose only job is to give tests and future reflection-based
/// checks a stable handle on this assembly, without reaching for a real class
/// that might later move or be renamed.
/// </summary>
public sealed class AssemblyMarker
{
}
